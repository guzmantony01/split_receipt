import 'package:flutter/material.dart';

class BreakDownPage extends StatelessWidget {
  const BreakDownPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('breakdown_page'));
  }
}